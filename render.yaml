services:
  - type: web
    name: revolt-backend
    runtime: node
    buildCommand: cd server && npm install
    startCommand: cd server && bash start-with-chrome.sh
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
